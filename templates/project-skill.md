---
name: <app-name>
description: Conventions for the <App Name> CxJS application. Use when working on its UI — routes, components, controllers, API calls or styles.
---

# <App Name>

<!-- DRAFT: starter for a per-application skill. Copy into your app's repository
     (e.g. `.agents/skills/<app-name>/` or `.claude/skills/<app-name>/`) and fill it in.
     The public `cxjs` skill covers the framework; this file covers what is specific to your app. -->
